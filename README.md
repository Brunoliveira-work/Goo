Para rodar a api use o comando na raiz:
- docker compose up --build

Para requisições:
 (POST)
 http://localhost:5000/api/data

 body:
   campo_busca: '' (substringo dos relatorios, CNPJ da empresa)
   
